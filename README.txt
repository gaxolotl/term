Prepare for running:

Windows:
Make sure pip, python and the rich library is installed with pip install rich
Linux:
Make sure pip, python and the rich library is installed with pip install rich
Run the following command:
chmod +x run.sh

Run the script based on your system:
On Windows: Double click run.bat or run it in cmd
On Linux: Run ./run.sh in the terminal